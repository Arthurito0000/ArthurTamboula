Place the two ADFuelCard images (from your attachments) into the project's public assets folder so the portfolio shows them in the "ADFuelCard pro" project gallery.

Destination folder:
public/assets/images/

Expected filenames (used by the site):
- adfuelcard-dashboard.png
- adfuelcard-login.png

Steps:
1. Copy the two PNG files from:
   c:\Users\arthu\Documents\dossier partarge\localhost_4200_login (1) - Copy.png
   c:\Users\arthu\Documents\dossier partarge\localhost_4200_login (1) - Copy.png
   (You may need to rename them on your side to match the expected filenames below.)

2. Rename them to the expected filenames and paste them into `public/assets/images/`.

3. Rebuild the site if needed:

```bash
npm run build
```

4. To preview locally after build:

```bash
npm run preview
```

If you want, I can add placeholder images to the repo now, or try to commit the real images if you confirm I should import the attached PNGs into `public/assets/images/` with the expected names.
